[package]
org = "anticaptrad"
name = "act-web-server-rs"
version = "0.1.0"
description = "Shared Auth protected Anticaptrad web gateway with four bounded API modes"
license = "MIT"
keywords = ["anticaptrad", "web", "rust", "seaorm", "nats", "shared-auth"]
language = "rust"

[package.repository]
vcs = "git"
url = "https://github.com/anticaptrad/act-web-server.rs"

[dependencies]
"anticaptrad/act-lib-core" = "^0.1.0"
"anticaptrad/act-orm-core" = "^0.1.0"
"anticaptrad/act-api-server-rs" = "^0.2.0"
"anticaptrad/act-interfaces" = "^0.1.0"
"shared-auth/shared-auth-clients" = "^0.1.0"
"oresoftware/next-loggers" = "^0.1.0"

[build]
command = "cargo build --release --locked"
outputs = ["target/release/act_web_server"]

[install]
adapter = "none"
dir = ".vendor/.zed"

[publish]
include_readme = true
tag_format = "v{version}"
smoke_test = "cargo test --all-targets --all-features --locked"
exclude = [".env", ".env.*", ".vendor/**", ".zed/**", "target/**", "**/*.log"]

[targets.rust]
dir = "."
adapter = "rust"

[scripts]
test = "cargo test --all-targets --all-features --locked"